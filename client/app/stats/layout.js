export const metadata = {
    title: "TextEmote - Stats",
    description: "Get your text emotion stats here!",
};

export default function StatsLayout({ children }) {
    return <section>{children}</section>;
}
