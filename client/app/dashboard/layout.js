export const metadata = {
    title: "TextEmote - Dashboard",
    description: "Get your emotion details right away!",
};

export default function DashboardLayout({ children }) {
    return <section>{children}</section>;
}
